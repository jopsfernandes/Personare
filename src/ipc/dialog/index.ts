import {
  selectAccountExportPath,
  selectBackupExportPath,
  selectBackupImportFile,
  selectImageFile,
  selectPdfFile,
} from "./handlers";

export const dialog = {
  selectAccountExportPath,
  selectBackupExportPath,
  selectBackupImportFile,
  selectImageFile,
  selectPdfFile,
};
